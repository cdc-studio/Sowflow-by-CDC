{
  "name": "@sowflow/api",
  "private": true,
  "version": "0.0.1",
  "main": "dist/src/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -w -p tsconfig.json",
    "prestart": "npm run build",
    "start": "func start",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@azure/cosmos": "^4.2.0",
    "@azure/functions": "^4.5.0",
    "@sentry/node": "^10.70.0",
    "@sowflow/shared-types": "workspace:*",
    "openai": "^7.5.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "azure-functions-core-tools": "^4.0.0",
    "typescript": "^5.5.0",
    "@types/node": "^20.14.0"
  }
}
